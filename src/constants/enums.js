// CommonJS export for use in Node CJS modules (e.g., Mongoose models)
const ROLES = ['student', 'teacher', 'staff', 'admin'];
const PROFILE_MODELS = ['StudentProfile', 'StaffProfile'];
const GENDERS = ['male', 'female', 'other'];

module.exports = {
  ROLES,
  PROFILE_MODELS,
  GENDERS,
};
